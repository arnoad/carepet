<%= link_to 'Back', pets_path, class: 'btn btn-secondary' %>
<p>Name: <%= @pet.name %></p>
<p>Age: <%= @pet.age %></p>
<p>Animal: <%= @pet.pet_type %></p>
<p>Breed: <%= @pet.breed %></p>
<p>Medical condition? <%= @pet.medical_cond %></p>
<p>Special needs? <%= @pet.special_needs %></p>

<%= link_to 'Edit', edit_pet_path, class: 'btn btn-primary' %>
<%= button_to 'Delete', pet_path(@pet), method: :delete, data: { turbo_confirm: "Are you sure?" } %>
